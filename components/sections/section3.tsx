import { SideBar } from "@/components/assetsec3/sidbar";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const Section3 = () => {
  return (
    <Card className="w-full max-w-4xl">
      <CardHeader>
        <CardTitle className="font-bold text-gray-800"></CardTitle>
      </CardHeader>
      <CardContent className="space-y-4">
        {/* <SideBar /> */}

      </CardContent>
    </Card>
  );
};

export default Section3;
